# Build output
build/
docs/
logs/
out/
cmake-build-*/
# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json
# Qt (user settings, generated moc/qrc/ui, baked shaders)
*.user
*.user.*
*.qmake.stash
.qmake.cache
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h
*_plugin_import.cpp
.qt/
*.qsb
# IDE / editors
.vscode/
.idea/
*.swp
*~
# OS
Thumbs.db
desktop.ini
.DS_Store
# Binaries / artifacts
*.exe
*.dll
*.pdb
*.ilk
*.log
# Save files (用户存档数据：每次游玩都变、二进制；绝不进 VCS。saves/ 下每世界一个 .sqlite)
saves/
*.sqlite
*.sqlite-journal
*.db
# t414 资源包目录（用户本地 MC 资源包；版权资产，绝不进 VCS。loader 仅运行期读取本地 PNG，不 bake 进 qrc）
resourcepacks/
# t415 用户设置（settings.json：含机器特定的资源包路径等；用户配置，绝不进 VCS）
settings.json
# Android (Phase 2)
android-build/
*.apk
*.aab
# Temp / process snapshots (sub-agent artifacts — never track; swept in by git add -A before)
_t*.txt
*_proc.txt
*_proc.*
# Python (sub-agent runs tools/*.py — bytecode cache, never track)
__pycache__/
*.pyc
